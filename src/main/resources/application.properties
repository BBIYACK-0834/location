# H2 Database 설정
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 콘솔 활성화 (웹에서 DB 확인 가능)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# DB 이름: mapsns
spring.datasource.url=jdbc:mysql://localhost:3306/mapsns?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
# 유저 아이디: mapsns
spring.datasource.username=mapsns

# 비밀번호: 1234
spring.datasource.password=1234

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==========================================
# 2. JPA 설정
# ==========================================
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# 테이블 생성 전략
# create: 서버 켤 때마다 초기화 (개발 초기 추천)
# update: 데이터 유지 (나중에 변경하세요)
spring.jpa.hibernate.ddl-auto=update

# SQL 로그 보기
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 한글 깨짐 방지
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true

# JPA 설정
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=seyyoeng546@gmail.com
spring.mail.password=nkql llgu zrhh ckpl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# 서버 포트
server.port=8080
